% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReconST_wrapper.R
\name{runReconST}
\alias{runReconST}
\title{Run the ReconST gene-panel selection pipeline}
\usage{
runReconST(
  input = NULL,
  output_dir = "reconst_results",
  embedding_size = 200L,
  num_epochs = 20L,
  batch_size = 256L,
  lr = 0.001,
  weight_decay = 1e-05,
  l_lambda = 1e-04,
  dropout = 0.05,
  leaky_slope = 0.3,
  threshold = 0.001,
  seed = 42L,
  device = NULL,
  envname = "giotto_reconst_env",
  gobject_sc = NULL,
  gobject_spatial = NULL,
  spat_unit = NULL,
  feat_type = NULL,
  spat_unit_sc = spat_unit,
  feat_type_sc = feat_type,
  subset_to_panel = FALSE,
  train_split = 0.8,
  min_genes_per_cell = 200L,
  min_cells_per_gene = 100L,
  normalize_target_sum = 10000,
  verbose = TRUE,
  num_threads = 1L
)
}
\arguments{
\item{input}{Output of \code{\link{getReconSTInput}} (a list with
\code{sc_h5ad} and \code{spatial_h5ad}), or any list/character vector
with those names.}

\item{output_dir}{Directory ReconST writes results into. Created if
missing.}

\item{embedding_size, num_epochs, batch_size}{Model dimensions and training
schedule, forwarded to the ReconST Python adapter.}

\item{lr, weight_decay, l_lambda}{Optimizer and L1 penalty settings.}

\item{dropout, leaky_slope}{Autoencoder activation settings.}

\item{threshold}{Select genes whose learned weight meets this threshold.}

\item{seed}{Random seed. Numeric values such as \code{42} are converted
to Python integers automatically.}

\item{device}{Optional torch device string ("cuda", "mps", "cpu").
Auto-detected when \code{NULL}.}

\item{envname}{Conda env to activate before any Python import. Use
\code{NULL} to use an already configured Python environment.}

\item{gobject_sc, gobject_spatial}{Reference scRNA-seq and spatial Giotto
objects. Both must use the same feature IDs; cell IDs need not match.}

\item{spat_unit, feat_type}{Spatial-object slots.}

\item{spat_unit_sc, feat_type_sc}{Reference-object slots.}

\item{subset_to_panel}{Keep only selected features in the returned object.}

\item{train_split}{Fraction of reference cells used for training.}

\item{min_genes_per_cell, min_cells_per_gene}{Reference-data filters.
Set either to \code{NULL} to skip that filter.}

\item{normalize_target_sum}{Reference-data normalization target; \code{NULL}
skips normalization. Spatial expression is evaluated as supplied.}

\item{verbose}{Print training progress.}

\item{num_threads}{Torch CPU threads during training. Defaults to one for
compatibility with R native libraries; the previous setting is restored.}
}
\value{
With Giotto objects, the spatial object with
  \code{reconst_importance}
  and \code{reconst_selected} feature metadata. With \code{input}, a list
  of artifact paths and summary statistics. Result files are saved in
  \code{output_dir} in both modes.
}
\description{
Supply two Giotto objects to export, run ReconST, and annotate the spatial
object in one call. Alternatively, supply \code{input} to run existing h5ad
files and return a summary, as in the original file-based API.
}
