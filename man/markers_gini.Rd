% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential_expression.R
\name{markers_gini}
\alias{markers_gini}
\alias{analyzeData,ANY,giniMarkersParam-method}
\title{Specificity Marker Detection (gini)}
\usage{
\S4method{analyzeData}{ANY,giniMarkersParam}(x, param, ..., groups = NULL, verbose = TRUE)
}
\arguments{
\item{x}{expression values — anything `analyzeData(x, featStatsParam)`
accepts, including a `matrix`, a `Matrix`, a `DelayedMatrix` (what
`expression_values = "scaled"` holds) or a disk-backed store.}

\item{param}{a [giniMarkersParam-class].}

\item{groups}{vector of group assignments, one per cell. Taken in column
order of `x`, unless the vector is named by cell ID, in which case it is
matched on identity. Naming is the safer form: the expression matrix and
the cell metadata a caller builds `groups` from are fetched independently
and need not share a cell order. `NA` excludes a cell from every group,
which is how a caller narrows to a subset without copying the object.}

\item{verbose}{report progress per group. `"one_vs_rest"` only.}
}
\value{
marker detection results
}
\description{
Detect marker features by how unevenly a feature is distributed across
groups, using the Gini coefficient.

Two coefficients are taken per feature, over the per-group mean expression
and over the per-group detection fraction:

\deqn{\LARGE
G_i = \frac{\sum_{a}\sum_{b} |v_{i,a} - v_{i,b}|}
           {2 G^2 \bar{v}_i}
}
Where:
\itemize{
\item (\eqn{v_{i,a}}) is the statistic for feature \eqn{i} in group \eqn{a} —
mean expression for \code{expression_gini}, detection fraction for
\code{detection_gini}
\item (\eqn{G}) is the number of groups
}

The statistic depends on the values only through the per-(feature, group)
mean and detection fraction, so the expression matrix is visited once
regardless of how many groups there are. That is what lets a streaming
backend supply gini markers with no code of its own — the pass is
\link[=featStatsParam-class]{featStatsParam}, and this method consumes its output.

Gini is \strong{scale-free}: a 0.001 vs 0.0001 difference between groups scores
identically to 100 vs 10. It therefore carries no magnitude term and will
rank near-noise features as perfectly specific, which is what
\code{min_expression} and \code{min_detection} exist to prevent. Its ceiling is
\eqn{(G-1)/G}, so a fixed \code{min_expression_gini} is not comparable across
runs with different group counts unless \code{min_length} is set.

Which expression values are scored is the caller's choice — the method uses
whatever matrix it is given.
}
\section{params}{


\tabular{ll}{
\code{comparison} \tab character (default = "pairwise"). \code{"pairwise"} scores
every group against the others at once; \code{"one_vs_rest"} scores each group
against the pooled remainder, one table per group. \cr
\code{min_expression} \tab numeric (default = 0.2). Minimum per-group mean
expression, gating the \code{expression} column. \cr
\code{min_detection} \tab numeric (default = 0.2). Minimum fraction of a
group's cells above \code{detection_threshold}, gating \code{detection}. \cr
\code{min_expression_gini} \tab numeric (default = -Inf). Minimum coefficient,
gating \code{expression_gini}. \cr
\code{min_detection_gini} \tab numeric (default = -Inf). Minimum coefficient,
gating \code{detection_gini}. \cr
\code{detection_threshold} \tab numeric (default = 0). Value above which a cell
counts as expressing a feature. Not a filter on returned rows. \cr
\code{min_length} \tab integer (default = 0). Pad each per-group vector to this
length with copies of its minimum before taking the coefficient, removing
the dependence on group count so scores compare across runs. \code{0} never
pads. \cr
\code{rank_score} \tab numeric (default = Inf). Keep a feature when its group is
within this rank for both expression and detection. \cr
\code{min_feats} \tab integer (default = 5). Keep this many top features per
group regardless of the gates, so a group is never empty.
}

The gates are OR'd with \code{min_feats}, so tightening them shrinks the result
toward \code{min_feats} per group and never below it.
}

\section{detection_margin}{


Both comparisons also return a \code{detection_margin} column: per (feature,
group), how many percentage points more of that group's cells detect the
feature than of the next-highest \strong{single} group's.

\deqn{\LARGE \mathrm{margin}(g,k) = d_{gk} - \max_{j \neq k} d_{gj}}

It answers a different question from the coefficients beside it. The gini
columns rank features \emph{within} a group; this measures whether a group has any
feature of its own at all, which is what distinguishes a real population from
an overclustered fragment. Two properties make it comparable across groups
where \code{comb_score} is not: the contrast is against one other group rather
than the pooled remainder, so it does not inherit the \eqn{N - n_k} term that
makes the one-vs-rest coefficients fall as a group grows; and it is not
rescaled within group, so \code{max(detection_margin)} per group means the same
thing in every group.

It is measured in percentage points and is negative where another group
detects the feature more often. A value of zero for a group's best feature
means no feature is more detected there than everywhere else.

Defaults here are \code{\link[=findGiniMarkers]{findGiniMarkers()}}'s. \code{\link[=findGiniMarkers_one_vs_all]{findGiniMarkers_one_vs_all()}}
passes \code{min_expression = 0.5}, \code{min_detection = 0.5} and \code{min_feats = 4}
explicitly; \code{comparison = "one_vs_rest"} does \strong{not} switch them for you.
}

\seealso{
\link{analyze_param}, \code{\link[=markersParam]{markersParam()}}, \code{\link[=findGiniMarkers]{findGiniMarkers()}},
\link[=featStatsParam-class]{featStatsParam}

Other marker detection parameters:
\code{\link{markers_scran}}
}
\concept{marker detection parameters}
