% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReconST_wrapper.R
\name{getReconSTInput}
\alias{getReconSTInput}
\title{Export paired Giotto objects to h5ad for ReconST}
\usage{
getReconSTInput(
  gobject_sc,
  gobject_spatial,
  output_dir,
  spat_unit = NULL,
  feat_type = NULL,
  expression_values = "raw",
  spat_unit_sc = spat_unit,
  feat_type_sc = feat_type
)
}
\arguments{
\item{gobject_sc}{A Giotto object holding the reference scRNA-seq data.}

\item{gobject_spatial}{A Giotto object holding the spatial data
(e.g. MERFISH). Must share the feature ID convention with
\code{gobject_sc}.}

\item{output_dir}{Directory to write the two h5ad files into.}

\item{spat_unit, feat_type}{Giotto slot identifiers; \code{NULL} uses each
object's active spatial unit and feature type.}

\item{expression_values}{Which expression matrix to export. ReconST
expects raw counts.}

\item{spat_unit_sc, feat_type_sc}{Reference-object slots. Default to the
spatial object's slot arguments; use these when the objects differ.}
}
\value{
A named list with \code{sc_h5ad} and \code{spatial_h5ad} paths.
}
\description{
ReconST needs paired scRNA-seq + spatial counts. This helper extracts
the raw count matrix and feature IDs from each Giotto object and writes
them as AnnData (\code{.h5ad}) files via reticulate/anndata.
}
