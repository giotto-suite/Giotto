% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/general_help.R
\name{writeClusterTreeQuery}
\alias{writeClusterTreeQuery}
\title{writeClusterTreeQuery}
\usage{
writeClusterTreeQuery(
  gobject,
  spat_unit = NULL,
  feat_type = NULL,
  expression_values = c("normalized", "scaled", "custom"),
  cluster_column,
  tree,
  splits = NULL,
  markers = NULL,
  gini_markers = NULL,
  node_markers = NULL,
  context = list(),
  margin_cut = 25,
  top_markers = 20L,
  top_gini = 5L,
  top_node = 10L,
  file_name = NULL
)
}
\arguments{
\item{gobject}{giotto object}

\item{spat_unit}{spatial unit}

\item{feat_type}{feature type}

\item{expression_values}{feature expression values to use. Consulted only
for evidence layers this function has to compute itself; one supplied by the
caller was already built on some choice of values.}

\item{cluster_column}{name of the cell metadata column holding the clusters}

\item{tree}{an `hclust` over the clusters, from [calculateClusterTree()]}

\item{splits}{the node table for `tree`, from [getDendrogramSplits()].
Derived from `tree` when not supplied.}

\item{markers}{one-vs-all marker table, from [findMarkers_one_vs_all()].
Computed when not supplied.}

\item{gini_markers}{gini marker table, from [findGiniMarkers_one_vs_all()].
Computed when not supplied; supplies the specificity flag.}

\item{node_markers}{output of [findNodeMarkers()]. Computed when not
supplied.}

\item{context}{named list of whatever is known about the sample --
`tissue`, `disease`, `assay`, anything else. Rendered as `Name: value`
lines at the top of the query.}

\item{margin_cut}{detection margin, in percentage points, below which a
cluster is flagged as having no feature of its own}

\item{top_markers, top_gini, top_node}{how many genes to list per cluster and
per branch side}

\item{file_name}{optional path to write the query to. The text is returned
either way.}
}
\value{
the query, as a character vector, invisibly
}
\description{
Build the annotation query for a cluster tree: the tree itself,
the markers separating the two sides of every branch, and the per-cluster
markers with a specificity flag.
}
\details{
Runs no LLM. It assembles the text to hand to one, exactly as
[writeChatGPTqueryDEG()] does for a flat marker list.

What it adds over a flat list is structure. The query asks for a label at
**every internal node** as well as every leaf, and a node's label must name
the clade beneath it rather than describe the split. That is what lets
[annotateClusterTree()] cut the answer at any granularity afterwards without
asking the model again.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

tree <- calculateClusterTree(g, cluster_column = "leiden_clus")
q <- writeClusterTreeQuery(g,
    cluster_column = "leiden_clus", tree = tree,
    context = list(tissue = "mouse brain")
)
head(q, 20)
}
\seealso{
[calculateClusterTree()], [findNodeMarkers()],
[annotateClusterTree()]
}
